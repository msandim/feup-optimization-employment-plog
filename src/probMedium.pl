% Slots input:
input_slots([10, 10, 10, 10, 10, 10, 10, 10]).
input_startWork(9).
input_endWork(16).

input_maxExtraWorkers(30).

% *** Full time workers: ***

input_fullSalaryPerHour(80).
input_fullBonus(40).

% Lunch hour:
input_lunchHourList([12,13]).

% *** Partial time workers: ***
input_partialWorkHours(3).
input_partialMaxWorkers(10).
input_partialWorkersRatio(30). %

input_partialSalaryPerHour(50).
input_partialBonus(60).

